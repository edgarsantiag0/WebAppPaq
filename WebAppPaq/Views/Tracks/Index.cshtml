@model IEnumerable<WebAppPaq.Models.Paq.Track>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            <th>
                Factura Id
            </th>
            <th>
             Detalle Factura Id
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DetalleFactura.Factura.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DetalleFactura.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado.Descripcion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TrackId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TrackId">Details</a> @*|
                <a asp-action="Delete" asp-route-id="@item.TrackId">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
